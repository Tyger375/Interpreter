var dictionary = {
    "hi": [
        "test",
        [
            "hi",
            "hi2"
        ]
    ],
    "heheheha": 2,
    "test":{
        "test":"heheheha",
        "wow": {
            "it works":[
                "yes",
                "les go",
                "ez"
            ]
        }
    },
    "str":"a string",
    "bool":true
}

var test_list = [
    {
        "heheheha":"omg"
    }
]

dictionary["hi"] = ["omg", "things"]

var keys = dictionary.keys()

outl(dictionary, keys.length())

var i = 0
var length = keys.length()
while (i < length)
{
    var key = keys[i]
    outl("dict", key, dictionary[key])
    i = i + 1
}

outl("hi", dictionary["test"]["wow"]["funziona"][0])

outl("test_list", test_list)

outl("test_list[0]", test_list[0])