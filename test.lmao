var hi = 0

var testbool = true

var boh = testbool + 2
out(boh)

func function(due)
{
    var ciao2 = due + 1
    return ciao2
}

var test = function("test")
out(test)
test = test + " " + function("ciao")
out(test)
out("test", test)
var input = "ciao"

out("input", input, type(input))

//var IN = in()

if (type(input) == "string" && hi == "ciao" && "string" == "string")
{
	out("oks")
}
else
{
    out("test")
}

while (hi < 5 && testbool == true)
{
    out("ciao", hi)
    hi = hi + 1
}

//for (int due = 0; due <= 0; due++)
//{

//}